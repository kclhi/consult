% args
a(c).
a(a).

% attacks
r(a(c),a(a)).
r(a(a),a(c)).

% values
val(a(a),value(m)).
val(a(c),value(p)).

% preferences
p(value(m),value(p)) :- aud(r1).
p(value(p),value(m)) :- aud(r2).

% view arguments -- uncomment one of them for a specific view
%aud(r1).
aud(r2).

% query: ./dlv ground.dl metalevel.dl DEMO/bittorrent-vaf.dl -filter=in

