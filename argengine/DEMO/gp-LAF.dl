% args
a(t1).
a(tls).

%a(gpref).

% attacks
r(a(tls),a(t1)).

% attacks on attacks
%d(a(gpref),a(tls),a(t1)).

arg(notdefeat(tls,t1)).

att(notdefeat(tls,t1), defeat(tls,t1)).

% query: ./dlv ground.dl metalevel.dl DEMO/gp-LAF.dl -filter=in

